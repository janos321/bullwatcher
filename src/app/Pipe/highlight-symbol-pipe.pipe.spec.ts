import { HighlightSymbolPipePipe } from './highlight-symbol-pipe.pipe';

describe('HighlightSymbolPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightSymbolPipePipe();
    expect(pipe).toBeTruthy();
  });
});
